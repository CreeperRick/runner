# runner

```
instance_runner/
├── runner.py          # Main Flask app + multiprocessing logic
├── templates/
│   └── dashboard.html # Simple web UI
└── tasks/             # (optional) store submitted code files```
